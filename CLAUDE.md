# CLAUDE.md — claude-code-hikitsugi

## 1. 前提（SKILL.md 1枚で動作する）

- Claude Code のプラグインです。中身は `skills/hikitsugi/SKILL.md` 1枚のみです
- Python やスクリプトは使用しません。通信も行いません
- 圧縮の要約には頼りません。いまのセッションの記憶と生ログから引き継ぎ書を作成します

## 2. 確認コマンド（変更したら実行する）

```
claude plugin validate .
```

## 3. 編集の約束（公開リポジトリとして守る3点）

- 実ログや実際の出力をコミットしません
- 個人のパス・氏名・会社名を記載しません。例示は `<ユーザー名>` のように伏せ字にします
- 機能を追加する場合も、まず SKILL.md に記載します。ファイルを増やすのは最後の手段とします
