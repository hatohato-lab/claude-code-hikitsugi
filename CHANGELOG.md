# 変更履歴

## 2026-09-20（v4.0.0・指し方の作り直し）

- **名前で呼ぶ方式をやめた。** v3 は新しいセッションで「◯◯を継いで」と言う設計だったが、実際に試すと届かなかった。Claude は自分のセッション名を見られない。名前は生ログに書かれるだけで、会話には渡らないため
- 指す手段を生ログのパスに変えた。新しいセッションへ渡すのは次の2行になった

  ```
  このログの "isCompactSummary":true を含む最後の行を読んで、続きから始めて
  <生ログのパス>
  ```

- 渡す側のスキル `skills/next/`（`/hikitsugi:next`）を足した。古いセッションで打つと、自分のログのパスを埋めた2行が出る
- Claude は自分のログの場所も知らないため、見張りフックが発言のたびに `hikitsugi_current.txt` へパスを書くようにした。`--current` はこれを読む。無ければ更新の新しい順で当てる
- スクリプトを `skills/hikitsugi/scripts/` から `scripts/` へ移し、2つのスキルから同じものを呼ぶようにした。入口を `--current` `--read` `--list` の3つに整理した（`--session` `--find` は廃止）
- 手順が4段（`/compact` → `/hikitsugi:next` → 新しいセッション → 貼る）であることを README の先頭に置いた。入れ方は後ろへ下げた
- 見張りの案内文からセッション名を外した。名前では指せないため
- README から実在のセッション名とIDを消した

## 2026-09-20（v3.0.0・作り直し）
- 生ログ124セッションを全件調べ、Claude Code が圧縮のたびに9つの見出しの要約（平均15,375字）を生ログへ書いていることを確認した。v2 が言葉の一致で組み立てていた内容は、この要約に最初から入っていた
- 方式を「要約を自分で作る」から「Claude Code が書いた最後の要約を取り出す」に変えた。`skills/hikitsugi/scripts/last_summary.py`（約120行）が `"isCompactSummary":true` の最後の行を出す。要約が無ければ「/compact を打って」と言って止まる
- 形をプラグインにした。`.claude-plugin/plugin.json` と `marketplace.json` を置き、GitHub から `/plugins`（VS Code）や `/plugin`（ターミナル）で直接入れられる。フックの登録も自動になるので install.py を廃止
- 見張り `watch.py` の基準を圧縮回数だけにした。ファイルのMBは文脈の重さと一致しないため外した
- 捨てたもの：言葉の一致による抽出（768行）、完了／失敗／対処の判定、digest.md、brief.md、`--ai-brief`、マスキング、`--since` `--max-chars` `--no-mask`。v2 の実体は退避してあり、git の履歴にも残る
- eval を作り直した（24項目 → 26項目）。作り物のログ2本で、取り出しと見張りの両方を判定する
- 詳細解説を `docs/index.html`（PlantUML 図3枚）にまとめた

## 2026-09-20（v2 最終）
- `--brief` を通信なしのローカル抽出に戻した。以前は `--brief` だけで `claude -p` を呼び、README と設計書の「完全ローカル・通信なし」と食い違っていた。AI要約は `--ai-brief` を明示したときだけ行う
- タイトルと書き込みファイルのパスも、抽出の時点でマスクするようにした。以前は handoff.md では隠れる文字列が、brief.md・貼り付け用の3行・AIへ渡す材料には生のまま出ていた
- tool_result のエラー本文を、500字へ切る前に全文マスクするようにした。以前は先に切っていたため、長い JWT 形式の文字列が途中で切れてマスク規則に一致せず、断片が残り得た

## 2026-09-12
- `watch.py` を追加（見張りフック）。発言のたびに自分の生ログを測り、圧縮1回以上または15MB超のときだけ「引き継いでください」の案内を1回出す。登録は settings.json の hooks.UserPromptSubmit に人が1本足す
- `--brief` を追加。生ログを分析し「決まったこと・いまの作業と止まっている場所・残作業と順番・成果物のパス・直近の指示と注意点」の5種だけを brief.md（3000字目安）にまとめ、新しいチャットに貼るだけで引っ越せる3行（前のチャット名・ID・メモの場所）を出す。理由：従来の handoff.md＋digest.md は100KB超で、新しいチャットが最初から重くなっていた
- 中身は生ログの要約を `claude -p` に第三者として読ませて書かせる。AIが使えないときは言葉の一致で抜き出し、質が低い旨を末尾に明記する
- 教訓（同日に実際に起きた）：中の claude に道具を持たせると要約せず hikitsugi 自体を実行して再帰する → `--tools ""`。MCP を読むと作業を始める → `--strict-mcp-config`。`--bare` は資格情報も飛ばして未ログインになる → 使わない

## 2026-09-02
- 仕組みの図解（ローカル描画のPlantUML SVG）をREADMEに追加

## 2026-08-31
- CLAUDE.md・design.md の古い「20項目」表記を実際の24項目に修正
- eval 見出しを「eval（機械判定24項目）」に統一・関連ツール（運用ファミリー6本）の表を追加
- CHANGELOG.md を追加（全リポジトリ方針）

## 2026-08-23
- eval を24項目に拡張・使用例を架空の題材に変更

## 2026-08-11
- 初版公開
